@model MVC_NorthWind.Models.Region

@{
    ViewBag.Title = "Regiones";
}

<head>
    <style>
        .card {
            margin: 3em;
            width: auto;
        }
        .error {
            color: red;
        }
    </style>
</head>

@using (Html.BeginForm("Create", "Regions", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <body>
        <div class="card container shadow p-3 mb-5">
            <h2 align="center">Formulario de Registro de Regiones</h2>
            <form>
                <div class="row section">
                    <div class="col-sm-6">
                        @Html.LabelFor(model => model.RegionID, htmlAttributes: new { @class = "control-label" })
                        @Html.EditorFor(model => model.RegionID, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.RegionID, "", new { @class = "text-danger" })
                    </div>
                    <div class="col-sm-6">
                        @Html.LabelFor(model => model.RegionDescription, htmlAttributes: new { @class = "control-label" })
                        @Html.EditorFor(model => model.RegionDescription, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.RegionDescription, "", new { @class = "text-danger" })
                    </div>
                    <p class="error">@ViewBag.ErrorMessage</p>
                    <div class="col-sm-12" style="margin-left:30em; margin-top: 2em; margin-bottom: 2em">
                        <input type="submit" value="Agregar" class="btn btn-primary" />
                    </div>
                </div>
            </form>
        </div>
    </body>
    <div class="btn btn-outline-primary">
        @Html.ActionLink("Atras", "Index")
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
